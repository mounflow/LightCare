# Gradle daemon 必须用 JDK 17+ 才能解析 Spring Boot 3.3 插件（默认 PATH 里的 Java 8 会导致 variant 解析失败）。
# server 的编译产物 JDK 21 由 toolchain 单独管（见 build.gradle.kts），与 daemon JVM 无关。
org.gradle.java.home=D:/SofewareHome/aboutP/OpenSdk17/install
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
